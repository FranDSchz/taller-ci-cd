name: CI/CD - Node.js App

on:
  push:
    branches: [ main ]

jobs:
  # El primer job sigue siendo el mismo, instala y cachea.
  build:
    name: 🔧 Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm' # La clave está aquí: activa la caché.

      - name: Install dependencies
        run: npm ci

      # Ya no subimos 'node_modules' como artefacto.
      # Podrías subir aquí artefactos de build si tu app los generara (ej: una carpeta 'dist').

      - name: Notify Slack - Build
        uses: 8398a7/action-slack@v3
        if: always()
        with:
          status: ${{ job.status }}
          author_name: '🔧 Build'
          job_name: 'build'
          text: 'Build `${{ github.repository }}` en `${{ github.ref }}`'
          fields: repo,ref,commit
          mention: 'here'
          if_mention: failure
          icon_emoji: ':hammer_and_wrench:'
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

  # Los jobs de lint y test ahora vuelven a tener sus propios pasos de instalación,
  # pero serán muy rápidos gracias a la caché.
  lint:
    name: 🧼 Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm' # Usamos la misma caché.

      # Este paso será casi instantáneo si la caché existe.
      - name: Install dependencies
        run: npm ci

      - name: Run ESLint
        run: npx eslint .

      - name: Notify Slack - Lint
        uses: 8398a7/action-slack@v3
        if: always()
        with:
          status: ${{ job.status }}
          job_name: 'lint'
          author_name: '🧼 Lint'
          text: 'Lint result for `${{ github.repository }}`'
          fields: repo,ref,commit
          icon_emoji: ':broom:'
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

  test:
    name: 🧪 Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm' # Y usamos la caché aquí también.

      # Este paso también será muy rápido.
      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Notify Slack - Test
        uses: 8398a7/action-slack@v3
        if: always()
        with:
          status: ${{ job.status }}
          author_name: '🧪 Test'
          job_name: 'test'
          text: 'Resultado de tests para `${{ github.repository }}`'
          fields: repo,ref,commit
          icon_emoji: ':test_tube:'
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}